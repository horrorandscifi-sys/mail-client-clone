# mail-client-clone
!!!ATTENTION!!! First, install Git and then run git init
git add from the command line.
git commit -m "Initial commit: creating the mail interface"
git branch -M main
git remote add origin https://github.com
git push -u origin main
